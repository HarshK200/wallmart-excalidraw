{
  "name": "ws-backend",
  "version": "0.1.0",
  "dependencies": {
    "@repo/db": "workspace:*",
    "@repo/typescript-config": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "tsc --watch & node --watch dist dist/index.js"
  }
}
